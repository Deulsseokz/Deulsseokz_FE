module.exports = {
  singleQuote: true, // Use single quotes instead of double quotes
  semi: true, // Add semicolons at the end of statements
  trailingComma: 'all', // Add trailing commas wherever possible (objects, arrays, etc.)
  bracketSpacing: true, // Add spaces inside object literal braces, e.g. { foo: bar }
  arrowParens: 'avoid', // Omit parentheses when a single parameter in arrow functions
  printWidth: 120,
};
